package nl.hugo.redbook.ch3

object Exercise24 {
  import List._
  def hasSubsequence[A](sup: List[A], sub: List[A]): Boolean =  ??? //sup match{
//    case Cons(s, up) =>
//  }
//    foldLeft(sup, sub)((sub,supp)=>foldLeft(sub, false)((a,su) => a && supp == su))
}